
a : 64 = 3;          // 3
b : 64 = 2;          // 2
c := a + b;          // 5
d := a * c;          // 15
e := b * d + c;      // 35
f := b * d - c;      // 25
g := e + a * b;      // 41
h := f - a * b;      // 19
j := a * b + c * d;  // 81
k := g * b - h * a;  // 25
l := j / 9 * (a + c); // 72
// 101
l + j + k - (a * d / b + c * d - g / b);
