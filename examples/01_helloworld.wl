// Simple hello world program
load std.io; // "load" loads libraries based on path (wl is the "directory", std is the "file")

// Prints the line
// a ID that ends with "#" denotes a macro statment
println# Hello, world!;
